version: '3.8'
services:
  golang:
    build:
      context: .
      dockerfile: ./docker/golang/Dockerfile
    tty: true
    volumes:
      - ./project:/go
      - pkg_store:/go/pkg
      - bin_store:/go/bin
  kube:
    image: kube:latest
    build:
      context: .
      dockerfile: ./docker/kube/Dockerfile
      target: stage1
    volumes:
      - ../terraform:/terraform
      - ~/.kube:/root/.kube
volumes:
  pkg_store:
  bin_store:
